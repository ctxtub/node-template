{
  "name": "node-frame",
  "version": "1.0.0",
  "description": "A node-frame build with mysql & redis & express，logged by log4js. / 一个简易的nodejs框架，支持mysql、redis数据库，由express提供路由分发，log4js日志记录",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.5",
    "log4js": "^2.3.4",
    "mysql": "^2.14.1",
    "redis": "^2.8.0"
  }
}
